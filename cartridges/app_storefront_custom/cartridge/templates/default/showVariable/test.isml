<isdecorate template="common/layout/page">
    <isif condition="${session.custom.x === 1}">
        ${session.custom.x + 1}
    <iselse/>
        <isset name="x" value="${1}" scope="session" />
        ${session.custom.x}
    </isif>
</isdecorate>